import billyManifest from './billy.manifest.json';
import type { AssemblyManifest } from '../types/assembly';

const manifest = billyManifest as unknown as AssemblyManifest;

export default manifest;
